export default function LoginPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Iniciar sesión</h1>
      {/* TODO (Ambos): formulario de login usando Better Auth */}
    </div>
  );
}
